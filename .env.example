
# ===========================================
# FRONTEND .env - CONFIGURACIÓN SEGURA
# Backend sirve frontend buildado (puerto único 3000)
# ===========================================

# API del backend (ruta relativa para seguridad)
VITE_API_URL=/api

# Configuración LTI (solo para mostrar en UI)
VITE_LTI_CONSUMER_KEY=tu_lti_consumer_key
VITE_LTI_ENABLED=true

# Canvas (solo URL base, token manejado en backend)
VITE_CANVAS_BASE_URL=https://ucanvas.instructure.com

# Aplicación
VITE_APP_NAME=MATUC Exercise Composer
VITE_NODE_ENV=development

# ===========================================
# NOTA: Esta configuración es para backend que sirve frontend
# - Solo puerto 3000 expuesto
# - Credenciales sensibles solo en backend
# - Rutas API relativas (/api/*)
# ===========================================